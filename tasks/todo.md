# Current Tasks

## In Progress
(none)

## Planned
(none)

## Completed
- 2026-05-05 — OTP screen migrated to `pinput` (^5.0.0). UI swapped to a single `Pinput` widget with custom `defaultPinTheme` / `focusedPinTheme` / `submittedPinTheme` matching the existing light/dark palette. All business logic (timer, verify, resend, navigation, error handling) untouched. `flutter analyze` clean.
