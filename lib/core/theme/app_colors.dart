import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // 👈 private constructor (no object creation)

  /// iOS Light Grey Background
  static const Color inputFieldBg = Color(0xFFF2F2F7);

  /// Primary Color (Purple - tumhare UI jaisa)
  static const Color primary = Color(0xFF7B2FF7);

  /// Text Colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;

  /// Border / Divider
  static const Color border = Color(0xFFE5E5EA);
}